services:
  logestoque:
    build:
      context: .
      dockerfile: dockerfile
    container_name: logestoque-beta
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 4004
    ports:
      - "4005:4004"
    volumes:
      - logestoque-beta-data:/app/gen/srv/db

volumes:
  logestoque-beta-data:
